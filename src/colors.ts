'use strict';
function red(text:string) {
    // should properly wrap the text in red escape code
    return text;
};

function yellow(text:string) {
    // should properly wrap the text in red escape code
    return text;
};

export {red, yellow};